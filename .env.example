# ===== Tailscale =====
TAILSCALE_HOSTNAME=n8n
TAILSCALE_AUTHKEY=tskey-<your-key-here>

# Expose a host port for n8n if desired (uncomment 'ports:' in ts-n8n service)
PUBLIC_PORT=5678

# ===== n8n =====
# Public hostname you’ll use to access n8n (DNS name or IP)
N8N_HOST=n8n.local
# http or https (use https when sitting behind a TLS terminator/reverse proxy)
N8N_PROTOCOL=http
# Container listen port (leave default unless you know you need to change it)
N8N_PORT=5678
# Full public webhook URL (optional; if set, overrides computed URL)
# Example: https://n8n.example.com/
N8N_WEBHOOK_URL=

# Timezone
N8N_TZ=America/Toronto

# Strongly recommended: set a long random value
N8N_ENCRYPTION_KEY=

# Telemetry / personalization
N8N_DIAGNOSTICS_ENABLED=false
N8N_PERSONALIZATION_ENABLED=false

# Execution mode: 'main' (default) or 'queue' (requires Redis)
N8N_EXECUTIONS_PROCESS=main
